<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulisboa.tecnico.cmov.airdesk" >

    <application
        android:name=".ApplicationContext"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LogInActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenu"
            android:label="@string/app_name"
            android:launchMode="singleTop" >
        </activity>
        <activity
            android:name=".ShowFileActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:parentActivityName=".MainMenu" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainMenu" />
        </activity>
        <activity
            android:name=".EditFileActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ShowFileActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ShowFileActivity" />
        </activity>
        <activity
            android:name=".TagsActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".NewWorkspaceActivity"
            android:label="@string/create_new_workspace"
            android:parentActivityName=".MainMenu" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainMenu" />
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".PrivacyActivity"
            android:label="@string/title_activity_workspace_privacy" >
        </activity>
        <activity
            android:name=".QuotaActivity"
            android:label="@string/title_activity_quota" >
        </activity>

        <activity
            android:name=".CreateFileActivity"
            android:label="@string/create_new_file" >
        </activity>

        <receiver
            android:name=".other.UpdateReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="pt.ulisboa.tecnico.cmov.airdesk.UPDATE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
